# Hermes → EasyTHS TradeClient 配置
# 复制到 data/easyths_trade.yaml 后修改：
#   cp /config/quant_scripts/easyths_trade.example.yaml \
#      /config/quant_scripts/data/easyths_trade.yaml

host: "127.0.0.1"
port: 7649  # 默认端口；实际以部署环境为准
scheme: "http"
api_key: ""

expected_mode: "paper"

easyths_path: "/config/easyths"

timeout: 60
